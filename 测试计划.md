# AI 智能教育课程平台 — 测试计划

> 版本: v1.0 | 日期: 2026-05-08 | 编写: 测试工程师

---

## 一、测试目标

验证平台全部功能模块的正确性、稳定性和可靠性，确保：
1. 11 个 Agent 各自的核心业务逻辑正确
2. Agent 调度引擎（注册、分发、聚合、依赖解析、错误处理）运转正常
3. 多 LLM Provider 层的切换、降级、模拟模式可用
4. API 层的接口格式、中间件、异常处理符合规范
5. 前端 9 个组件的渲染和交互逻辑正确
6. 数据仓库层的 CRUD 操作正确

---

## 二、测试范围总览

| 层级 | 模块 | 测试类型 | 优先级 |
|------|------|---------|--------|
| L1 - Agent 引擎 | 注册表 / 分发器 / 聚合器 / 依赖解析 / 错误处理 | 单元测试 | P0 |
| L2 - 业务 Agent | 知识拆分 / 难度标注 / 知识讲解 / 练习题生成 / 批改 / 问答 / 路径规划 / 跳级建议 / 鼓励奖励 / 行为记录 / 行为分析 | 单元测试 | P0 |
| L3 - LLM Provider | OpenAI / 智谱GLM / Kimi / Qwen / 豆包 / Mock客户端 / 工厂 / 配置管理 | 单元测试 | P0 |
| L4 - API 层 | 端点接口 / JWT认证 / 限流 / 日志 / 统一响应格式 | 集成测试 | P1 |
| L5 - 数据仓库 | 用户仓库 / 知识仓库 / 进度仓库 | 单元测试 | P1 |
| L6 - 前端组件 | 9 个 React 组件 + API 服务层 + 类型定义 | 组件测试 | P2 |
| L7 - 端到端 | 完整业务流程（主题提交 → 知识拆分 → 讲解 → 练习 → 批改） | E2E 测试 | P1 |

---

## 三、整体测试（冒烟测试）

### 3.1 目的

用最少用例快速验证系统核心链路是否通畅，作为后续分项测试的前置条件。

### 3.2 冒烟用例

| 编号 | 用例 | 操作 | 预期结果 | 涉及模块 |
|------|------|------|---------|---------|
| S-01 | 后端启动 | `uvicorn app.main:app --port 8000` | 服务启动无报错，`/health` 返回 `{"status":"healthy"}` | main.py |
| S-02 | API 文档可访问 | 浏览器打开 `/docs` | Swagger UI 正常渲染，列出全部接口 | FastAPI |
| S-03 | Agent 模拟模式 | 运行 `test_agents.py`（无 API Key） | 11 个 Agent 全部 PASS | 全部 Agent + Mock |
| S-04 | Provider 注册完整性 | 导入 llm_providers，检查 5 个 Provider 已注册 | `list_registered_providers()` 返回 5 项 | factory.py |
| S-05 | 前端编译 | `cd frontend && npm install && npm run build` | 编译成功无报错 | 全部前端组件 |
| S-06 | 统一响应格式 | GET `/` | 返回 `{code:0, message, data, meta}` 格式 | response.py |

### 3.3 执行命令

```bash
# S-01
cd backend && uvicorn app.main:app --port 8000 &
curl http://localhost:8000/health

# S-03
cd backend && python tests/test_agents.py

# S-04
cd backend && python -c "
from app.agents.llm_providers import list_registered_providers
print('已注册Provider:', [p.value for p in list_registered_providers()])
assert len(list_registered_providers()) == 5, 'Provider注册不完整'
print('PASS')
"

# S-05
cd frontend && npm install && npm run build
```

---

## 四、分项测试

### 4.1 L1 - Agent 引擎基础设施

#### 4.1.1 Agent 注册表 (registry.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| R-01 | 正常注册 | 合法 AgentRegistration | 返回 AgentInfo，agent_count +1 | 注册成功 |
| R-02 | 重复注册 | 相同 agent_id 注册两次 | 抛出 AgentAlreadyExistsError | 唯一性约束 |
| R-03 | 参数校验 | agent_id 为空 / capabilities 为空 / endpoint 为空 | 抛出 AgentRegistryError | 必填字段校验 |
| R-04 | 容量限制 | 注册超过 max_agents(100) 个 | 抛出 AgentRegistryError | 上限保护 |
| R-05 | 按能力查找 | 注册 KNOWLEDGE_SPLIT 能力后查找 | 返回对应 AgentInfo 列表 | 能力索引正确 |
| R-06 | 按类型查找 | 注册 type="KnowledgeSplitAgent" 后查找 | 返回对应列表 | 类型索引正确 |
| R-07 | 状态更新 | 将 Agent 状态改为 BUSY | is_available 仍为 True（未满载） | 状态与可用性逻辑 |
| R-08 | 负载更新 | current_tasks 设为 max_concurrent | is_available 变为 False | 负载因子计算 |
| R-09 | 心跳与清理 | 心跳超时后调用 cleanup_stale_agents | Agent 状态变为 OFFLINE | 超时清理 |
| R-10 | 注销 | 注销已注册 Agent | agent_count -1，索引清除 | 注销完整性 |

#### 4.1.2 任务分发器 (dispatcher.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| D-01 | 正常分发 | TaskRequest(KNOWLEDGE_SPLIT) + 已注册 Agent | 返回 TaskResult(SUCCESS) | 端到端分发 |
| D-02 | 无可用 Agent | TaskRequest 但无匹配 Agent | 返回 TaskResult(FAILED) | 降级处理 |
| D-03 | 超时处理 | TaskRequest(timeout_ms=1) | 返回 TaskResult(TIMEOUT) | 超时机制 |
| D-04 | 批量分发 | 3 个 TaskRequest | 返回 3 个 TaskResult | 并发分发 |
| D-05 | 任务取消 | dispatch 后立即 cancel_task | 返回 TaskResult(CANCELLED) | 取消机制 |
| D-06 | 过期清理 | 创建过期任务后 cleanup | 过期任务被清理 | 内存泄漏防护 |

#### 4.1.3 依赖解析器 (dependency.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| DP-01 | 无依赖任务 | 3 个独立 TaskRequest | 1 个批次，可并行 | 基础拓扑排序 |
| DP-02 | 串行依赖 | A→B→C | 3 个批次，每批 1 个任务 | 依赖链正确 |
| DP-03 | 并行+串行混合 | A→C, B→C | 2 个批次：[A,B]→[C] | 并行优化 |
| DP-04 | 循环依赖检测 | A→B→C→A | 抛出 CircularDependencyError | 环检测 |
| DP-05 | 缺失依赖 | 任务依赖不存在的 task_id | 抛出 MissingDependencyError | 完整性校验 |
| DP-06 | 空任务列表 | 空列表 | 返回空批次列表 | 边界条件 |

#### 4.1.4 结果聚合器 (aggregator.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| AG-01 | 全部成功 | 3 个 SUCCESS 的 TaskResult | success=True，output_data 合并 | ALL_SUCCESS 策略 |
| AG-02 | 部分失败 | 2 SUCCESS + 1 FAILED | PARTIAL_OK 策略下 success=True | 部分成功策略 |
| AG-03 | 全部失败 | 3 个 FAILED | success=False | 失败处理 |
| AG-04 | 空结果 | 空列表 | success=False，error_message 非空 | 边界条件 |
| AG-05 | 自定义合并 | merge_func 拼接字符串 | output_data 为拼接结果 | 自定义合并 |
| AG-06 | 顺序聚合 | SequentialAggregator + 3 步结果 | output_data 为最后一步的输出 | 数据传递 |

#### 4.1.5 错误处理器 (error_handler.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| EH-01 | 可重试错误 | ConnectionError + max_retries=3 | 重试 3 次后降级 | 重试机制 |
| EH-02 | 不可重试错误 | ValueError | 不重试，直接降级 | 错误分类 |
| EH-03 | 指数退避 | TRANSIENT 错误 + EXPONENTIAL 策略 | 延迟 1s → 2s → 4s | 退避算法 |
| EH-04 | 降级策略 | 重试耗尽 + RETURN_DEFAULT | 返回 default_value | 降级执行 |
| EH-05 | 错误指标 | 连续 5 次失败 | error_rate 正确计算 | 指标统计 |
| EH-06 | 装饰器模式 | @with_error_handling 修饰函数 | 异常时自动重试/降级 | 装饰器集成 |

---

### 4.2 L2 - 业务 Agent 单元测试

#### 4.2.1 知识拆分 Agent (knowledge_split.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| KS-01 | 正常拆分 | topic_name="Python基础", description="..." | blocks 非空，total_points > 0 | 核心功能 |
| KS-02 | 三层结构 | 同上 | blocks → points → components 层级完整 | 数据结构 |
| KS-03 | 空主题 | topic_name="" | 抛出参数校验异常 | 边界条件 |
| KS-04 | 长描述 | 5000 字 description | 正常返回，不截断 | 大输入处理 |

#### 4.2.2 难度标注 Agent (difficulty_tag.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| DT-01 | 正常标注 | 3 个 knowledge_points | annotations 数量匹配 | 核心功能 |
| DT-02 | 难度枚举 | 同上 | difficulty ∈ {EASY, MEDIUM, HARD} | 枚举合法性 |
| DT-03 | 重要性枚举 | 同上 | importance ∈ {LOW, MEDIUM, HIGH} | 枚举合法性 |
| DT-04 | 空列表 | 空 knowledge_points | 抛出异常或返回空 | 边界条件 |

#### 4.2.3 知识讲解 Agent (content_explain.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| CE-01 | 正常讲解 | point_info + BEGINNER + SIMPLE | sections 非空，estimated_minutes > 0 | 核心功能 |
| CE-02 | 风格适配 | teaching_style=ACADEMIC | 内容风格偏学术 | 风格参数生效 |
| CE-03 | 等级适配 | user_level=ADVANCED | 内容深度适合高级 | 等级参数生效 |

#### 4.2.4 练习题生成 Agent (exercise_generate.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| EG-01 | 正常生成 | point_info + total_questions=5 | total_count=5 | 数量控制 |
| EG-02 | 题型分布 | 同上 | 包含多种 question_type | 题型多样性 |
| EG-03 | 难度分布 | 同上 | difficulty_distribution 非空 | 难度分布 |

#### 4.2.5 题目批改 Agent (answer_grade.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| AG-01 | 全对 | 正确答案列表 | correct_count = total, percentage=100 | 客观题批改 |
| AG-02 | 全错 | 错误答案列表 | correct_count=0, percentage=0 | 错误处理 |
| AG-03 | 混合 | 部分正确 | percentage 介于 0-100 | 混合批改 |
| AG-04 | 空答案 | answers 为空 | 抛出异常 | 边界条件 |

#### 4.2.6 问答 Agent (qa_answer.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| QA-01 | 正常问答 | question + context | summary 非空，related_knowledge 非空 | 核心功能 |
| QA-02 | 问题分类 | 同上 | question_type ∈ 枚举值 | 分类正确 |
| QA-03 | 空问题 | question="" | 抛出异常 | 边界条件 |

#### 4.2.7 路径规划 Agent (path_planning.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| PP-01 | 正常规划 | user_id + knowledge_structure + user_profile | nodes 非空，estimated_total_hours > 0 | 核心功能 |
| PP-02 | 节点顺序 | 同上 | nodes 按学习顺序排列 | 顺序正确 |

#### 4.2.8 跳级建议 Agent (skip_suggest.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| SS-01 | 高掌握度 | mastery_scores 全部 > 0.85 | suggestions 非空，confidence=HIGH | 核心功能 |
| SS-02 | 低掌握度 | mastery_scores 全部 < 0.5 | suggestions 为空或 confidence=LOW | 保守策略 |

#### 4.2.9 鼓励奖励 Agent (reward_generate.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| RG-01 | 完成知识点 | trigger_scene=POINT_COMPLETED | rewards 非空，total_points_earned > 0 | 核心功能 |
| RG-02 | 连续学习 | streak_days=7 | 鼓励文案包含连续学习相关内容 | 场景适配 |

#### 4.2.10 行为记录 Agent (behavior_record.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| BR-01 | 正常记录 | event 对象 | record_id 非空，status=SUCCESS | 核心功能 |
| BR-02 | 缺失字段 | event 缺少 user_id | 抛出参数校验异常 | 参数校验 |

#### 4.2.11 行为分析 Agent (behavior_analysis.py)

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| BA-01 | 正常分析 | user_id + events 列表 | profile.activity_level 和 mastery_level 有值 | 核心功能 |
| BA-02 | BKT 更新 | 多次正确/错误事件 | 掌握度概率正确更新 | BKT 算法 |
| BA-03 | 空事件 | events 为空 | 返回默认画像 | 边界条件 |

---

### 4.3 L3 - LLM Provider 层

| 编号 | 用例 | 输入 | 预期 | 验证点 |
|------|------|------|------|--------|
| LP-01 | Provider 注册 | 导入全部 Provider 模块 | 5 个 Provider 均已注册到工厂 | 注册完整性 |
| LP-02 | Mock 客户端 | MockLLMClient.agenerate([prompt]) | 返回 JSON 格式的模拟响应 | 模拟模式 |
| LP-03 | Mock 知识拆分响应 | prompt 含"拆解" | 返回 blocks 结构的 JSON | 响应格式匹配 |
| LP-04 | Mock 讲解响应 | prompt 含"讲解" | 返回 sections 结构的 JSON | 响应格式匹配 |
| LP-05 | Mock 练习题响应 | prompt 含"练习题" | 返回 questions 结构的 JSON | 响应格式匹配 |
| LP-06 | AgentLLMClient 无 Provider | 不配置任何 API Key | 自动降级到 Mock 模式 | 降级机制 |
| LP-07 | AgentLLMClient 兼容性 | 传入 llm_client 旧参数 | 兼容旧版 agenerate 接口 | 向后兼容 |
| LP-08 | 配置管理 | 设置环境变量 ZHIPU_API_KEY | get_llm_config().get_config(ZHIPU) 非空 | 环境变量加载 |
| LP-09 | 配置文件加载 | load_from_json_file(config.json) | 配置正确解析 | 文件配置 |
| LP-10 | 工厂缓存 | get_provider() 调用两次 | 返回同一实例 | 单例缓存 |

---

### 4.4 L4 - API 层集成测试

| 编号 | 用例 | 方法/路径 | 预期 | 验证点 |
|------|------|----------|------|--------|
| API-01 | 根路径 | GET `/` | `{name, version, status:"running"}` | 服务状态 |
| API-02 | 健康检查 | GET `/health` | `{status:"healthy"}` | 健康检查 |
| API-03 | 推荐主题 | GET `/api/v1/topics/recommend?count=3` | code=0，data 包含 3 个主题 | 接口格式 |
| API-04 | 推荐数量边界 | GET `/api/v1/topics/recommend?count=11` | 返回默认 3 个（count 上限 10） | 参数校验 |
| API-05 | 知识体系 | GET `/api/v1/topics/{id}/structure` | code=0，data 包含 blocks | 接口格式 |
| API-06 | 统一响应格式 | 任意成功接口 | `{code:0, message, data, meta:{timestamp, version}}` | 响应规范 |
| API-07 | 404 处理 | GET `/api/v1/not-exist` | 返回 404 格式错误响应 | 异常处理 |

---

### 4.5 L5 - 数据仓库层

| 编号 | 用例 | 模块 | 操作 | 预期 |
|------|------|------|------|------|
| RE-01 | 用户创建 | user_repo | create_user({name, email}) | 返回用户对象，id 非空 |
| RE-02 | 用户查询 | user_repo | get_user(user_id) | 返回对应用户 |
| RE-03 | 用户不存在 | user_repo | get_user("not-exist") | 返回 None |
| RE-04 | 知识保存 | knowledge_repo | save_topic({topic_name, blocks}) | 返回保存结果 |
| RE-05 | 进度更新 | progress_repo | update_progress(user_id, point_id, status) | 进度正确更新 |

---

### 4.6 L6 - 前端组件测试

| 编号 | 组件 | 测试点 | 预期 |
|------|------|--------|------|
| FE-01 | TopicInput | 输入文本 → 点击提交 | 调用 submitTopic API |
| FE-02 | TopicRecommendations | 组件渲染 | 显示推荐主题列表 |
| FE-03 | TeachingPanel | 传入讲解数据 | 正确渲染章节内容 |
| FE-04 | QAPanel | 输入问题 → 提交 | 调用 askQuestion API |
| FE-05 | ProgressPanel | 传入进度数据 | 正确渲染进度百分比 |
| FE-06 | PanoramaProgress | 传入知识体系 | 渲染三层结构 |
| FE-07 | LearningPathPanel | 传入路径数据 | 渲染学习路径节点 |
| FE-08 | LearningHistory | 传入历史数据 | 渲染历史列表 |
| FE-09 | SkipTest | 传入测试题目 | 渲染题目和选项 |

---

### 4.7 L7 - 端到端测试

| 编号 | 场景 | 流程 | 预期 |
|------|------|------|------|
| E2E-01 | 完整学习流程 | 主题提交 → 知识拆分 → 知识讲解 → 练习生成 → 答题批改 | 全链路数据正确传递 |
| E2E-02 | 问答流程 | 知识讲解中提问 → 问答 Agent 返回答案 | 答案与知识点关联 |
| E2E-03 | 跳级流程 | 高掌握度 → 跳级建议 → 跳级测试 → 通过 | 路径缩短 |
| E2E-04 | 激励流程 | 完成知识点 → 触发奖励 → 积分增加 | 积分正确累计 |

---

## 五、测试执行顺序

```
阶段 1: 冒烟测试（S-01 ~ S-06）         ← 前置条件，全部通过后才继续
    │
    ▼
阶段 2: L1 Agent 引擎（R/D/DP/AG/EH）   ← 基础设施，Agent 运行的前提
    │
    ▼
阶段 3: L3 LLM Provider（LP-01~10）      ← Provider 层，Agent 的依赖
    │
    ▼
阶段 4: L2 业务 Agent（KS/DT/CE/EG...）  ← 核心业务逻辑
    │
    ▼
阶段 5: L5 数据仓库（RE-01~05）           ← 数据持久化
    │
    ▼
阶段 6: L4 API 层（API-01~07）           ← 接口集成
    │
    ▼
阶段 7: L7 端到端（E2E-01~04）           ← 全链路验证
    │
    ▼
阶段 8: L6 前端组件（FE-01~09）           ← UI 层（可并行）
```

---

## 六、测试环境要求

| 项目 | 要求 |
|------|------|
| Python | 3.10+ |
| Node.js | 16+ |
| 测试框架 | pytest + pytest-asyncio（后端）、Jest（前端） |
| API Key | 冒烟测试和单元测试无需 API Key（Mock 模式） |
| 数据库 | 当前使用内存存储，无需 PostgreSQL |
| Redis | 当前未强制依赖，可跳过 |

---

## 七、通过标准

| 指标 | 标准 |
|------|------|
| 冒烟测试 | 6/6 通过 |
| L1 引擎测试 | 100% 通过 |
| L2 Agent 测试 | 100% 通过 |
| L3 Provider 测试 | 100% 通过 |
| L4 API 测试 | 100% 通过 |
| L5 仓库测试 | 100% 通过 |
| L7 端到端 | 100% 通过 |
| 整体通过率 | >= 95% |

---

## 八、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 真实 API 调用不稳定 | LLM 相关测试偶发失败 | 优先使用 Mock 模式，真实 API 测试标记为可选 |
| Agent 响应格式变化 | Mock 响应与真实不匹配 | Mock 响应与 Agent 解析逻辑同步更新 |
| 前端测试环境差异 | 组件测试本地通过但 CI 失败 | 统一 Node 版本，使用 Docker 容器 |
| 并发测试竞态条件 | 注册表/分发器偶发失败 | 添加适当的等待和重试机制 |
