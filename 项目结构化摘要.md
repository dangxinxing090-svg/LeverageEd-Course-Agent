# AI智能教育课程平台 - 项目结构化摘要

## 项目概述

**项目名称**: AI智能教育课程平台  
**版本**: V1.6.0  
**更新日期**: 2026-05-16

**核心功能**:
- 智能知识点拆分与讲解（7维度结构化卡片展示）
- 自适应学习路径规划
- BKT/DKT知识掌握度追踪
- 用户行为分析与画像生成
- 记忆压缩与数据归档
- 练习题生成与答案批改
- 知识组件级学习进度追踪（学习中/完成/做题中/练习通过）

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React 18 + TypeScript + Vite |
| 后端 | Python 3.10 + FastAPI |
| 数据库 | PostgreSQL (SQLAlchemy ORM) |
| LLM | 多Provider支持（智谱、通义千问、Kimi、豆包、OpenAI、DeepSeek） |
| 流式传输 | SSE (Server-Sent Events) |

---

## 项目结构

```
workspace/
├── backend/                    # 后端服务
│   ├── app/
│   │   ├── agents/            # Agent模块（核心业务逻辑）
│   │   │   ├── behavior/      # U-029 行为记录 / U-030 行为分析 / U-031 记忆压缩
│   │   │   ├── incentive/     # 激励系统
│   │   │   ├── knowledge/     # 知识拆分 / 难度标签
│   │   │   ├── learning/      # 统一教学 / 练习生成 / 答案批改 / QA问答
│   │   │   ├── llm_providers/ # LLM Provider适配层
│   │   │   └── path_planning/ # 学习路径规划 / 跳级建议
│   │   ├── api/v1/            # API层
│   │   │   ├── endpoints/     # API端点
│   │   │   └── middleware/    # 中间件（认证、日志、限流、响应格式）
│   │   ├── core/              # 核心模块
│   │   │   ├── bkt.py         # BKT贝叶斯知识追踪
│   │   │   ├── dkt.py         # DKT深度知识追踪
│   │   │   └── config.py      # 配置管理
│   │   ├── db/                # 数据库连接
│   │   ├── models/            # SQLAlchemy数据模型
│   │   ├── repositories/      # 数据访问层
│   │   └── services/          # 服务层
│   └── requirements.txt
├── frontend/                   # 前端应用
│   └── src/
│       ├── api/               # API封装
│       ├── components/        # React组件
│       │   ├── TeachingPanel/    # 教学面板（流式讲解、问答、练习）
│       │   ├── ExercisePanel/    # 练习面板
│       │   ├── QAPanel/          # 问答面板
│       │   ├── LearningPathPanel/# 学习路径面板
│       │   ├── PanoramaProgress/ # 全景知识图谱
│       │   ├── ProgressPanel/    # 进度面板
│       │   ├── LearningHistory/  # 学习历史
│       │   ├── SkipTest/         # 跳级测试
│       │   ├── TopicInput/       # 主题输入
│       │   └── TopicRecommendations/ # 主题推荐
│       ├── services/          # API服务
│       ├── types/             # TypeScript类型定义
│       └── App.tsx            # 主应用（路由、状态管理）
└── prompts/                    # Prompt模板
```

---

## 核心模块说明

### 后端模块

#### 1. Agents（Agent层）

| Agent | 文件 | 功能 |
|-------|------|------|
| U-029 行为记录 | `agents/behavior/behavior_record.py` | 采集、清洗、存储用户行为事件 |
| U-030 行为分析 | `agents/behavior/behavior_analysis.py` | 四维度分析（时间/内容/练习/路径）、BKT建模 |
| U-031 记忆压缩 | `agents/behavior/memory_compression.py` | 数据温度分类、每日/每周摘要聚合 |
| 知识拆分 | `agents/knowledge/knowledge_split.py` | LLM驱动的知识点拆分 |
| 难度标签 | `agents/knowledge/difficulty_tag.py` | 知识点难度评估 |
| 统一教学 | `agents/learning/unified_teaching_agent.py` | 7维度知识讲解、练习题生成、答案批改 |
| QA问答 | `agents/learning/qa_answer.py` | 用户问题回答 |
| 学习路径规划 | `agents/path_planning/path_planning.py` | 个性化学习路径推荐 |
| 跳级建议 | `agents/path_planning/skip_suggest.py` | 跳级测试建议 |
| 激励生成 | `agents/incentive/reward_generate.py` | 学习激励内容生成 |

#### 2. Core（核心算法）

| 模块 | 文件 | 功能 |
|------|------|------|
| BKT | `core/bkt.py` | 贝叶斯知识追踪（4参数：p_init, p_transit, p_slip, p_guess） |
| DKT | `core/dkt.py` | 深度知识追踪（知识状态转移预测、薄弱组件识别） |

#### 3. Services（服务层）

| 服务 | 文件 | 功能 |
|------|------|------|
| BKTService | `services/bkt_service.py` | BKT与数据库交互、掌握度更新 |
| DKTService | `services/dkt_service.py` | DKT预测、学习路径推荐 |
| UserProfileService | `services/user_profile_service.py` | 用户画像生成、持久化 |
| CompressionService | `services/compression_service.py` | 记忆压缩执行、统计查询 |

#### 4. Models（数据模型）

| 模型 | 表名 | 功能 |
|------|------|------|
| UserProfile | user_profiles | 用户画像（学习能力、风格标签、BKT参数） |
| BehaviorLog | behavior_logs | 行为日志（学习/提问/练习/跳级/交互） |
| LearningProgress | learning_progress | 学习进度（组件掌握度、BKT概率、学习状态） |
| ExerciseHistory | exercise_history | 练习历史（答题记录、错误类型） |

**LearningProgress 状态字段说明**：
- `not_started`: 未开始
- `in_progress`: 学习中（用户点击进入组件）
- `learn_completed`: 完成学习（用户滚动讲解到底部）
- `practicing`: 做题中（提交答案但不完全正确）
- `exercise_passed`: 练习通过（提交答案完全正确）
- `mastered`: 已掌握（BKT掌握度达到阈值）

#### 5. API端点

| 路径 | 功能 |
|------|------|
| `/api/v1/topics/*` | 主题管理（创建、状态查询、结构获取） |
| `/api/v1/learning/*` | 学习相关（讲解流、练习生成、答案提交） |
| `/api/v1/behavior/*` | 行为记录、掌握度查询、用户画像、DKT预测、记忆压缩、组件状态更新 |
| `/api/v1/users/*` | 用户管理 |

### 前端模块

#### 1. 页面组件

| 组件 | 文件 | 功能 |
|------|------|------|
| LearnPage | `App.tsx` | 学习页主组件（状态管理、知识结构加载、行为记录） |
| PanoramaProgress | `components/PanoramaProgress/` | 全景知识图谱（列表视图，显示学习状态标签） |

#### 2. 学习页状态持久化

学习页使用 **localStorage** 实现状态持久化，确保用户在页面导航、刷新浏览器后仍能恢复之前的学习状态。

| 存储键 | 说明 |
|--------|------|
| `currentTopicId` | 当前学习主题ID |
| `currentTopicName` | 当前学习主题名称 |
| `knowledgeBlocks_{topicId}` | 知识结构数据 |
| `learnPage_overview_{topicId}` | 主题全景介绍 |
| `learnPage_overviewMode_{topicId}` | 是否显示全景介绍模式 |
| `learnPage_componentIndex_{topicId}` | 当前学习组件索引 |
| `learn_progress_{componentId}` | 知识组件学习进度（学习中/完成/做题中/练习通过） |

**状态恢复逻辑**：
1. 组件初始化时，从 localStorage 读取 `overviewMode` 和 `currentComponentIndex`
2. 若有保存值，恢复之前的学习状态（显示对应知识组件讲解）
3. 若无保存值，默认显示全景介绍
4. 状态变化时自动保存到 localStorage

#### 3. 功能组件

| 组件 | 功能 |
|------|------|
| TeachingPanel | 7维度知识讲解卡片、问答对话、练习题内嵌 |
| ExercisePanel | 练习题作答、答案提交 |
| QAPanel | 问答互动 |
| LearningPathPanel | 学习路径展示、进度追踪 |
| ProgressPanel | 学习进度统计 |
| LearningHistory | 学习历史记录 |
| SkipTest | 跳级测试 |
| TopicInput | 主题输入 |
| TopicRecommendations | 主题推荐 |

---

## 数据流

### 学习流程

```
用户输入主题
    ↓
POST /api/v1/topics → Agent知识拆分 → 返回topicId
    ↓
GET /api/v1/topics/{id}/status → 轮询状态
    ↓
GET /api/v1/topics/{id}/structure → 获取知识结构（三层：板块→知识点→知识组件）
    ↓
GET /api/v1/learning/knowledge/components/{id}/explanation
    ↓
非流式结构化讲解（7维度JSON）→ TeachingPanel卡片展示
    ↓
用户完成学习 → POST /api/v1/behavior/component/status → 更新学习状态
    ↓
用户点击练习 → GET /api/v1/exercises/generate?component_id=xxx
    ↓
根据 component_id 查找 component_name + point_name + block_name → 调用 LLM 出题
    ↓
返回针对该知识组件的练习题
```

### 知识组件学习状态追踪流程

```
用户点击知识组件 A
    ↓
App.tsx → markLearning('A')
    ↓ 同时执行
    ├→ localStorage: learn_progress_A = { learnStatus: 'learning' }
    └→ POST /api/v1/behavior/component/status { status: 'in_progress' }
         ↓
         数据库 LearningProgress 表 upsert 记录

用户滚动讲解到底部
    ↓
TeachingPanel → markLearnCompleted('A')
    ↓ 同时执行
    ├→ localStorage: learn_progress_A = { learnStatus: 'completed' }
    └→ POST /api/v1/behavior/component/status { status: 'learn_completed' }
         ↓
         数据库更新状态

用户提交练习题（不完全正确）
    ↓
ExercisePanel → markPracticing('A')
    ├→ localStorage: learn_progress_A = { exerciseStatus: 'practicing' }
    └→ POST → 数据库: status = 'practicing'

用户提交练习题（完全正确）
    ↓
ExercisePanel → markExercisePassed('A')
    ├→ localStorage: learn_progress_A = { exerciseStatus: 'passed' }
    └→ POST → 数据库: status = 'exercise_passed'

回到全景知识页
    ↓
GET /api/v1/topics/{topicId}/structure
    ↓
后端从数据库读取组件状态，合并到返回数据中
    ↓
PanoramaProgress 渲染状态标签（学习中/完成/做题中/练习通过）
```

### 出题数据流

```
前端：传入 component_id（如 "comp-6ad91267"）
    ↓
后端 find_component_info(component_id)
    ↓
从内存缓存或文件缓存查找，返回：
  - component_name（如 "函数的定义"）
  - point_name（如 "函数的本质与作用"）
  - block_name（如 "Python函数基础核心"）
    ↓
Prompt 包含：
  - 知识组件：{component}（当前正在学习的具体组件）
  - 所属知识点：{knowledge}（组件的上级知识点）
  - 所属板块：{block}
    ↓
LLM 生成针对该知识组件的练习题
```

### 行为记录流程

```
用户行为（学习/练习/提问）
    ↓
前端调用 recordBehavior()
    ↓
POST /api/v1/behavior/record
    ↓
后端保存 BehaviorLog
    ↓
如果是练习行为 → BKTService.update_progress_after_attempt()
    ↓
更新 LearningProgress（bkt_p_known, status）
```

---

## 记忆系统架构（P0-P5）

| 阶段 | 内容 | 状态 |
|------|------|------|
| P0 | 数据库Schema、行为记录API、基础表 | ✅ 完成 |
| P1 | 前端行为记录集成（learn/practice/question） | ✅ 完成 |
| P2 | BKT算法、掌握度计算 | ✅ 完成 |
| P3 | 用户行为分析、画像生成 | ✅ 完成 |
| P4 | DKT知识状态转移预测 | ✅ 完成 |
| P5 | 记忆压缩、数据归档 | ✅ 完成 |
| P6 | 知识组件级学习状态追踪（数据库持久化） | ✅ 完成 |

---

## API接口清单

### 行为记录相关

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/behavior/record` | 记录行为（自动更新BKT） |
| POST | `/behavior/component/status` | 更新知识组件学习状态（写入数据库） |
| GET | `/behavior/logs/{user_id}` | 查询行为日志 |
| GET | `/behavior/stats/{user_id}` | 行为统计 |
| GET | `/behavior/mastery/{user_id}/{component_id}` | 组件掌握度 |
| GET | `/behavior/mastery/summary/{user_id}` | 掌握度汇总 |
| GET | `/behavior/profile/{user_id}` | 用户画像 |
| POST | `/behavior/profile/{user_id}/analyze` | 触发画像分析 |
| GET | `/behavior/dkt/{user_id}` | DKT预测 |
| GET | `/behavior/dkt/{user_id}/weak` | 薄弱组件 |
| GET | `/behavior/dkt/{user_id}/recommend` | 学习路径推荐 |
| GET | `/behavior/dkt/{user_id}/compare` | BKT/DKT对比 |
| GET | `/behavior/compression/stats` | 压缩统计 |
| POST | `/behavior/compression/execute` | 执行压缩 |
| GET | `/behavior/compression/summaries/{user_id}` | 每日摘要 |

---

## 配置说明

### 后端环境变量

```bash
# LLM Provider配置
ZHIPU_API_KEY=xxx
DASHSCOPE_API_KEY=xxx
KIMI_API_KEY=xxx
DOUBAO_API_KEY=xxx
OPENAI_API_KEY=xxx
DEEPSEEK_API_KEY=xxx

# 数据库配置
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_education
```

### 前端环境变量

```bash
VITE_API_URL=http://localhost:8000
```

---

## 运行命令

```bash
# 启动后端
cd backend && source venv/bin/activate
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 启动前端
cd frontend && npm run dev
```

---

## 文档索引

| 文档 | 路径 |
|------|------|
| 架构设计文档 | `AI智能教育课程平台_项目架构设计文档_V1.2.docx` |
| 项目立项书 | `立项策划/AI智能教育课程平台策划书.md` |
| 测试计划 | `测试计划.md` |
| 知识图谱设计 | `frontend/src/components/PanoramaProgress/KnowledgeGraphDesign.md` |

---

## 更新日志

### V1.6.0 (2026-05-16)

**知识组件级学习状态追踪系统上线**：
- 新增知识组件学习状态追踪功能，支持四种状态：学习中、完成、做题中、练习通过
- 状态存储从纯前端 localStorage 升级为前后端同步 + 数据库持久化
- 全景知识页知识组件显示状态标签（橙色：学习中/做题中，绿色：完成/练习通过）
- 状态同步机制：前端状态变更时自动同步到后端，后端查询知识结构时合并状态数据

**技术改动**：
- `backend/app/models/progress.py` — 扩展 status 字段支持新状态值
- `backend/app/api/v1/endpoints/behavior.py` — 新增 POST /component/status 端点
- `backend/app/api/v1/endpoints/knowledge_cache.py` — 新增组件状态内存缓存函数
- `backend/app/api/v1/endpoints/topics.py` — get_topic_structure 从数据库读取组件状态
- `frontend/src/api/behavior.ts` — 新增 updateComponentStatus 函数
- `frontend/src/utils/progressStorage.ts` — 4 个状态函数增加后端同步调用
- `frontend/src/components/PanoramaProgress/PanoramaProgress.tsx` — 去掉状态图标，只显示标签

### V1.5.0 (2026-05-12)

**新增 DeepSeek API 支持**：
- 新增 DeepSeek LLM Provider，支持 deepseek-chat / deepseek-v4-flash 模型
- 后端配置系统扩展，支持 DEEPSEEK_API_KEY / DEEPSEEK_MODEL 环境变量
- 综合练习 Prompt 优化，强制只出问答题，禁止选择题/判断题/填空题

### V1.4.1 (2026-05-11)

**修复内容**：
- 修复练习题字段名不匹配问题：LLM 返回的字段名为 `content`，但前端期望 `question_text`
- 在 `generate_exercises` 接口中添加字段映射转换：`content` → `question_text`

**修改文件**：
- `backend/app/api/v1/endpoints/learning.py` — 添加字段映射逻辑

### V1.4.0 (2026-05-11)

**修复内容**：
- 修复出题粒度太粗的问题：将出题依据从知识点级别（point_name）改为知识组件级别（component_name）
- 现在出题基于当前正在学习的具体知识组件，而不是其所属的知识点
- 题目与当前学习内容紧密相关，不再出现"永远出差不多类型的题目"

**修改文件**：
- `backend/app/api/v1/endpoints/learning.py` — generate_exercises 和 submit_exercises 传入 component_name
- `backend/app/agents/learning/unified_teaching_agent.py` — generate_exercises 方法签名 + EXERCISE_PROMPT 使用 component 变量

### V1.3.3 (2026-05-11)

**修复内容**：
- 修复后端重启后出题与当前知识组件不匹配的问题：`find_component_info` 在内存缓存未命中时，增加从持久化文件加载的兜底逻辑，找到后自动恢复到内存缓存

**修改文件**：
- `backend/app/api/v1/endpoints/knowledge_cache.py` — `find_component_info` 增加文件加载兜底

### V1.3.2 (2026-05-11)

**修复内容**：
- 提高重点知识点标注标准：引入"承重墙"隐喻，要求必须同时满足2条以上严格标准才可标为重点
- 调整重点知识点目标比例：从20%（范围15%-25%）调整为30%（范围25%-35%）
- 难点知识点标注标准不变

**修改文件**：
- `backend/app/agents/learning/unified_teaching_agent.py` — SPLIT_PROMPT 标注标准 + 后处理比例参数

### V1.3.1 (2026-05-11)

**修复内容**：
- 修复练习题与当前知识组件不匹配的问题：前端传入 `pointId`（知识点ID），但后端期望 `component_id`（知识组件ID），导致查找失败

**修改文件**：
- `frontend/src/types/index.ts` — ExercisePanelProps 参数名改为 componentId
- `frontend/src/components/ExercisePanel/ExercisePanel.tsx` — 接收 componentId 参数
- `frontend/src/services/api.ts` — generateExercises 和 submitAnswers 参数改为 componentId
- `frontend/src/App.tsx` — 传入 componentId 而不是 pointId
- `backend/app/api/v1/endpoints/learning.py` — API 参数名改为 component_id

### V1.3.0 (2026-05-11)

**跳级建议系统重新设计**：
- 将跳级评估逻辑从"多条件阈值判断"改为"重要性+难度二维矩阵"
- 跳级建议只针对当前知识组件和下一个知识组件显示
- 新增三种跳级建议类型：建议跳过、用户自主决定、不建议跳过

**决策矩阵**：

| | 不重要 (is_key_point=false) | 重要 (is_key_point=true) |
|---|---|---|
| **简单** | 建议考虑跳级 | 用户自主决定 |
| **中等** | 用户自主决定 | 不建议跳级 |
| **困难** | 用户自主决定 | 不建议跳级 |

**修改文件**：
- `backend/app/agents/path_planning/skip_suggest.py` — 重构跳级评估逻辑
- `backend/app/agents/path_planning/__init__.py` — 更新导出
- `backend/app/agents/__init__.py` — 更新导出
- `frontend/src/types/index.ts` — 新增跳级建议类型定义
- `frontend/src/components/LearningPathPanel/LearningPathPanel.tsx` — 新版跳级建议渲染
- `frontend/src/components/LearningPathPanel/styles.css` — 新增跳级建议样式
- `frontend/src/App.tsx` — 添加跳级建议计算和跳过操作

### V1.2.1 (2026-05-11)

**修复内容**：
- 修复学习页状态保持问题：从全景知识页点击导航栏"学习"返回时，学习页现在能正确恢复之前的学习状态（显示之前的知识组件讲解），而不是重新加载显示全景介绍

**技术改动**：
- 将学习页状态存储从 `sessionStorage` 改为 `localStorage`，实现持久化保存
- 修复 `overviewMode` 和 `currentComponentIndex` 的初始化逻辑，在 `useState` 初始化时直接从 localStorage 读取保存的状态
- 简化状态恢复逻辑，移除不再需要的 `isRestoredRef` 标记

**修改文件**：
- `frontend/src/App.tsx`

---

*最后更新: 2026-05-16*
